import { createZoo } from '../lib/zoo/zoo';
import { Dog } from '../lib/animals/dog';

const newZoo:Dog[] = createZoo();

// console.log(newZoo[0].deliberateError);
console.log(49);
console.log(newZoo);
